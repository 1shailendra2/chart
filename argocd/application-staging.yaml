apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: msgapp-staging
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  
  source:
    repoURL: https://github.com/1shailendra2/chart.git
    targetRevision: main
    path: helm-chart
    helm:
      valueFiles:
        - values.yaml
        - environments/values-staging.yaml
  
  destination:
    server: https://kubernetes.default.svc
    namespace: msgapp-staging
  
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
  
  ignoreDifferences:
    - group: apps
      kind: Deployment
      jsonPointers:
        - /spec/replicas
