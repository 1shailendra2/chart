config:
  target: "http://localhost:9000"
  phases:
    - duration: 60
      arrivalRate: 10
      rampTo: 100
      name: "Moderate Load"
  defaults:
    headers:
      Content-Type: "application/json"
scenarios:
  - name: "Health Check"
    flow:
      - get:
          url: "/health"